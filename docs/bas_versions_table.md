# Таблица состава БАС с версиями и моделями

Основание:
- `sim_stack.json:3-27`
- `validation/gz_x500_model_spec.json:1-90`
- `docs/drone_architecture.md:5-15,40,49-56,63-82,97-123`
- `docs/report_alignment_audit.md:195-205,222-251,253-260`
- `security_agent/collector.py:138-145`
- `security_agent/audit.py:16-43`

| Подсистема | Состав | Версия / модель | Функции |
| --- | --- | --- | --- |
| Подсистема управления полетом | `Pixhawk 6C`; `PX4 SITL`; команды управления; 4 моторных канала | `Pixhawk 6C`; `PX4 SITL`; точная версия PX4 не подтверждена | Центральный полетный компонент стенда; прием команд управления; формирование управляющих воздействий `motor_speed` |
| Подсистема навигации и позиционирования | `navsat`; `imu`; `magnetometer`; `air_pressure`; навигационное состояние PX4 | `gz_x500`: `navsat` / `imu` / `magnetometer` / `air_pressure`; `M8N GPS` не подтвержден | Формирование навигационных данных `position`, `gps_info`, `health` для PX4 и телеметрии |
| Подсистема обмена данными и телеметрии | `MAVLink/UDP`; `MavlinkGateway`; API- и GCS-каналы; `TelemetryCollector` | `MAVLink/UDP`; API `14541/14540`; GCS `14552/14550`; версия MAVLink не подтверждена | Передача команд и телеметрии; разделение `API/GCS`; сбор метрик канала; выборочная блокировка `SERIAL_CONTROL` |
| Подсистема взаимодействия с оператором | оператор; `QGroundControl`; ручной UDP-link | `QGroundControl AppImage`; точная версия QGroundControl не подтверждена | Ввод команд оператора; отображение телеметрии и состояния |
| Подсистема хранения эксплуатационных и конфигурационных данных | `sim_stack.json`; `gz_x500_model_spec.json`; параметры PX4; миссия PX4; журналы `telemetry/protocol/security` | `JSON` / `JSONL`; аппаратный носитель не подтвержден | Хранение конфигурации стенда; параметров; миссии; журналов работы |
| Подсистема электропитания и исполнительных механизмов | `battery telemetry`; `command/motor_speed`; 4 моторных канала | `gz_x500`: `4 motor channels`; `battery telemetry`; `PM02/PDB/ESC` не подтверждены | Контроль уровня заряда и напряжения; передача управляющих воздействий на моторные каналы |
| Стенд моделирования | `Gazebo`; модель `gz_x500`; `PX4 SITL` | `gz_x500`; `PX4_SIM_MODEL=x500`; точные версии Gazebo и PX4 не подтверждены | Среда моделирования и виртуальная модель аппарата |
| Контур мониторинга ИБ | `SecurityMonitorApp`; `TelemetryCollector`; `StateGuard`; `ThreatDetector`; `RiskEngine`; `Responder`; `AuditLogger` | Состав модулей подтвержден; точные версии не подтверждены | Сбор телеметрии и протокольных событий; контроль параметров и миссии; оценка риска; журналирование; частичное реагирование |
